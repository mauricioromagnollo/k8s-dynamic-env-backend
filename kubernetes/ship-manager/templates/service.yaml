apiVersion: v1
kind: Service
metadata:
  name: {{ include "ship-manager.fullname" . }}
  labels:
    {{- include "ship-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ship-manager.selectorLabels" . | nindent 4 }}
